# Skill: Terminal Execution & File Management

## Capability
*   Manage `ResumeMatchV2` directory structures and execute Gradle builds.
*   Run `supabase db push` and `pytest` for durable execution verification.

## Guardrails
*   **Request Review**: You are FORBIDDEN from running destructive commands (`rm`, `drop table`) without human approval[cite: 1, 2].
*   **Dry Run**: Always output the proposed terminal command to the Antigravity Inbox first[cite: 2].